# نظام PEX — نشر الباك إند الجديد (Vercel + قاعدة بيانات حقيقية)

المرة دي مفيش أي اتصال بجوجل خالص — الموقع وقاعدة البيانات كلهم على نفس السيرفر (فيرسل)، وده اللي هيحل مشكلة "مفيش اتصال بالسيرفر" من جذرها. بس النشر شكله شوية مختلف عن المرة اللي فاتت لأن فيه أكواد سيرفر (مش مجرد ملف HTML)، فهنستخدم GitHub كمحطة وسيطة زي ما اتفقنا. الخطوات:

## 1) اعمل مستودع (repository) جديد على GitHub
- ادخل على https://github.com/new
- اكتب اسم زي `pex-weekly-checkin`
- سيبه **Public** أو **Private** زي ما تحب (Private أفضل شوية لأنه فيه كلمة سر لوحتك جوه الكود)
- من غير ما تحط أي تيك (README, .gitignore, license) — سيبها فاضية
- دوس **Create repository**

## 2) ارفع الملفات
بعتلك كل حاجة في ملف واحد مضغوط اسمه `pex-vercel-project.zip` — فك الضغط عنه الأول على جهازك (كليك يمين > Extract / استخراج)، هتلاقي جواه الملفات والمجلد `api`.

بعدين هتلاقي نفسك في صفحة فاضية على GitHub فيها رابط "uploading an existing file" — دوس عليه (أو من فوق Add file > Upload files).
اسحب كل الملفات والمجلد اللي فككت ضغطهم مع بعض في نفس الوقت (مش ملف الـ zip نفسه):
- `index.html`
- `package.json`
- `schema.sql`
- مجلد `api` (اللي فيه `action.js`)
- `.gitignore`

اكتب أي رسالة في خانة commit تحت (زي "أول نسخة") ودوس **Commit changes**.

## 3) اربط المستودع بفيرسل
- افتح https://vercel.com/new
- هتلاقي مستودع `pex-weekly-checkin` ظاهر في القايمة — دوس **Import** جنبه
- سيب كل الإعدادات زي ما هي ودوس **Deploy**
- هياخد دقيقة والصفحة تشتغل، وهيديك رابط زي `https://pex-weekly-checkin.vercel.app`

## 4) اعمل قاعدة البيانات (خطوة واحدة، من غير كود)
- من مشروعك في فيرسل، روح تاب **Storage**
- دوس **Create Database**، اختار **Neon** (أو **Postgres**) من القايمة
- اقبل الشروط واختار المنطقة، وسمّيها أي اسم
- دوس **Connect** واختار مشروعك (`pex-weekly-checkin`)، ووافق يتوصل على كل البيئات (Development / Preview / Production)

## 5) جهّز الجداول (مرة واحدة بس)
- لسه إنت في تاب Storage، دوس على قاعدة البيانات اللي عملتها، وهتلاقي تاب اسمه **SQL Editor** أو **Query**
- افتح ملف `schema.sql` اللي معاك، انسخ محتواه كله، الصقه في الـ Query Editor، ودوس **Run**
- لو حصل أي رسالة نجاح أو "no rows" يبقى تمام، الجداول اتعملت

## 6) لازم تعمل Redeploy مرة واحدة
- روح تاب **Deployments** في مشروعك، دوس على التلات نقط `...` بجانب آخر نشرة، واختار **Redeploy**
- ده عشان الموقع ياخد متغير قاعدة البيانات اللي اتضاف بعد أول نشرة

## 7) جرب النظام
- افتح `الرابط-بتاعك/?admin=1`
- ادخل بكلمة السر: `Pex2026Coach!`
- ضيف عميل تجريبي وجرب تملى الفورم من لينكه، وشوف بيظهر في اللوحة ولا لأ

## تغيير كلمة السر بعدين
- من GitHub، افتح `api/action.js`، دوس على أيقونة القلم (Edit) فوق يمين
- غيّر السطر `const ADMIN_PASSWORD = 'Pex2026Coach!';` لكلمة السر الجديدة
- احفظ (Commit changes) — فيرسل هينشر النسخة الجديدة أوتوماتيك من غير ما تعمل حاجة تانية

## لو حبيت تضيف سؤال جديد في الفورم بعدين
قولّي وقتها، وهظبط الفورم في `index.html` وعمود جديد في قاعدة البيانات مع بعض، وابعتلك الملفات المحدّثة ترفعها بنفس طريقة اليوم.
